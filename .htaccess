# FastCGI configuration for Flask app
RewriteEngine On
RewriteBase /

# Don't rewrite static files
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else through FastCGI
RewriteRule ^(.*)$ dispatch.fcgi/$1 [QSA,L]
